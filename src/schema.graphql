 type Query {
    info: String!
    users: [User!]!
  }

  type Mutation {
    post(name: String!, surname: String!,profession: String!, onvacation: Boolean!): User!
  }

  type User {
    id: ID!
    name: String!
    surname: String!
    profession: String!
    onvacation: Boolean!
  }